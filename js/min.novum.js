!function() {
function createParticles(){const container=document.getElementById('particles');const particleCount=10;const fragment=document.createDocumentFragment();for(let i=0;i<particleCount;i=i+1){const particle=document.createElement('div');particle.classList.add('particle');const size=Math.random()*150+50;const posX=Math.random()*100;const posY=Math.random()*100;const delay=Math.random()*5;const duration=Math.random()*10+10;particle.style.width=`${ size }px`;particle.style.height=`${ size }px`;particle.style.left=`${ posX }%`;particle.style.top=`${ posY }%`;particle.style.animationDelay=`${ delay }s`;particle.style.animationDuration=`${ duration }s`;fragment.appendChild(particle)}container.appendChild(fragment)}function setupSlider(){const slides=document.querySelectorAll('.slide');const dotsContainer=document.querySelector('.slider-dots');const prevArrow=document.querySelector('.arrow.prev');const nextArrow=document.querySelector('.arrow.next');let currentSlide=0;const totalSlides=slides.length;slides.forEach((_,index)=>{const dot=document.createElement('button');dot.classList.add('dot');if(index===0){dot.classList.add('active')}dot.addEventListener('click',()=>goToSlide(index));dotsContainer.appendChild(dot)});const dots=document.querySelectorAll('.slider-dots button');function goToSlide(index){slides[currentSlide].classList.remove('active');dots[currentSlide].classList.remove('active');currentSlide=index;slides[currentSlide].classList.add('active');dots[currentSlide].classList.add('active')}function nextSlide(){goToSlide((currentSlide+1)%slides.length)}function prevSlide(){goToSlide((currentSlide-1+slides.length)%slides.length)}nextArrow.addEventListener('click',nextSlide);prevArrow.addEventListener('click',prevSlide);const sliderInterval=setInterval(nextSlide,7000);window.addEventListener('beforeunload',()=>{clearInterval(sliderInterval)});const navLinks=document.querySelectorAll('.vertical-nav a');navLinks.forEach(link=>{if(link.getAttribute('href')!=='#hero'){link.addEventListener('click',()=>{clearInterval(sliderInterval)})}})}function setupScrollEffect(){const header=document.getElementById('header');let lastScrollTop=0;window.addEventListener('scroll',()=>{const scrollTop=window.pageYOffset||document.documentElement.scrollTop;if(scrollTop>100){header.classList.add('scrolled')}else{header.classList.remove('scrolled')}lastScrollTop=scrollTop})}function setupSectionSwitching(){const verticalNavLinks=document.querySelectorAll('.vertical-nav a');const headerNavLinks=document.querySelectorAll('nav ul li a');const sections=document.querySelectorAll('section, .hero-slider');let currentSectionIndex=0;const sectionsArray=Array.from(sections);let isAnimating=false;sectionsArray.forEach(section=>section.classList.remove('active'));sectionsArray[0].classList.add('active');verticalNavLinks.forEach(link=>link.classList.remove('active'));headerNavLinks.forEach(link=>link.classList.remove('active'));if(verticalNavLinks[0]){verticalNavLinks[0].classList.add('active')}if(headerNavLinks[0]){headerNavLinks[0].classList.add('active')}function activateSection(sectionId){if(window.innerWidth<=992){const targetSection=document.getElementById(sectionId);if(targetSection){window.scrollTo({top:targetSection.offsetTop-60,behavior:'smooth'})}return}if(isAnimating){return}const targetIndex=sectionsArray.findIndex(section=>section.id===sectionId);if(targetIndex===-1||targetIndex===currentSectionIndex){return}currentSectionIndex=targetIndex;updateActiveNav();isAnimating=true;const currentSection=sectionsArray.find(section=>section.classList.contains('active'));const newSection=sectionsArray[targetIndex];if(currentSection){currentSection.classList.remove('active')}setTimeout(()=>{newSection.classList.add('active');setTimeout(()=>{isAnimating=false},1200)},600)}verticalNavLinks.forEach(link=>{link.addEventListener('click',function(e){e.preventDefault();const sectionId=this.getAttribute('href').substring(1);activateSection(sectionId)})});headerNavLinks.forEach(link=>{link.addEventListener('click',function(e){e.preventDefault();const sectionId=this.getAttribute('href').substring(1);activateSection(sectionId)})});window.addEventListener('wheel',function(e){if(window.innerWidth<=992){return}e.preventDefault();if(isAnimating){return}if(e.deltaY>0){if(currentSectionIndex<sectionsArray.length-1){activateSection(sectionsArray[currentSectionIndex+1].id)}}else{if(currentSectionIndex>0){activateSection(sectionsArray[currentSectionIndex-1].id)}}},{passive:false});window.addEventListener('keydown',function(e){if(window.innerWidth<=992){return}if(isAnimating){return}if(e.key==='ArrowDown'||e.key==='PageDown'){e.preventDefault();if(currentSectionIndex<sectionsArray.length-1){activateSection(sectionsArray[currentSectionIndex+1].id)}}else if(e.key==='ArrowUp'||e.key==='PageUp'){e.preventDefault();if(currentSectionIndex>0){activateSection(sectionsArray[currentSectionIndex-1].id)}}});function updateActiveNav(){const activeSection=sectionsArray[currentSectionIndex];verticalNavLinks.forEach(link=>{link.classList.toggle('active',link.getAttribute('href').substring(1)===activeSection.id)});headerNavLinks.forEach(link=>{link.classList.toggle('active',link.getAttribute('href').substring(1)===activeSection.id)})}if(window.innerWidth<=992){window.addEventListener('scroll',function(){const sections=document.querySelectorAll('section, .hero-slider');const navLinks=document.querySelectorAll('.vertical-nav a');let current='';sections.forEach(section=>{const sectionTop=section.offsetTop;const sectionHeight=section.clientHeight;if(pageYOffset>=(sectionTop-sectionHeight/3)){current=section.getAttribute('id')}});navLinks.forEach(link=>{link.classList.remove('active');if(link.getAttribute('href').substring(1)===current){link.classList.add('active')}})})}let ticking=false;window.addEventListener('scroll',function(){if(!ticking){window.requestAnimationFrame(function(){ticking=false});ticking=true}},{passive:true})}function setupTouchNavigation(){if(window.innerWidth<=992){return}let touchStartY=0;let touchEndY=0;const minSwipeDistance=50;document.addEventListener('touchstart',function(e){touchStartY=e.changedTouches[0].screenY},{passive:true});document.addEventListener('touchend',function(e){touchEndY=e.changedTouches[0].screenY;handleSwipe()},{passive:true});function handleSwipe(){const distance=touchStartY-touchEndY;if(Math.abs(distance)<minSwipeDistance){return}const navLinks=document.querySelectorAll('.vertical-nav a');const sections=document.querySelectorAll('section, .hero-slider');const sectionsArray=Array.from(sections);let currentIndex=0;sectionsArray.forEach((section,index)=>{if(section.classList.contains('active')){currentIndex=index}});if(distance>0){if(currentIndex<sectionsArray.length-1){navLinks[currentIndex+1].click()}}else{if(currentIndex>0){navLinks[currentIndex-1].click()}}}}function setupFooter(){const footer=document.getElementById('footer');const footerToggle=document.querySelector('.footer-toggle');footerToggle.addEventListener('click',function(){if(footer.style.transform==='translateY(0px)'){footer.style.transform='translateY(100%)'}else{footer.style.transform='translateY(0px)'}})}function setupMobileMenu(){const menuToggle=document.querySelector('.mobile-menu-toggle');const nav=document.querySelector('nav');const topContacts=document.querySelector('.top-contacts');menuToggle.addEventListener('click',function(){menuToggle.classList.toggle('open');nav.classList.toggle('open');topContacts.classList.toggle('open')});const menuLinks=document.querySelectorAll('nav ul li a');menuLinks.forEach(link=>{link.addEventListener('click',function(){menuToggle.classList.remove('open');nav.classList.remove('open');topContacts.classList.remove('open')})})}function setupMobileScrollSpy(){if(window.innerWidth<=992){const sections=document.querySelectorAll('section, .hero-slider');const navLinks=document.querySelectorAll('.vertical-nav a');window.addEventListener('scroll',function(){let current='';sections.forEach(section=>{const sectionTop=section.offsetTop;const sectionHeight=section.clientHeight;if(pageYOffset>=(sectionTop-sectionHeight/3)){current=section.getAttribute('id')}});navLinks.forEach(link=>{link.classList.remove('active');if(link.getAttribute('href').substring(1)===current){link.classList.add('active')}})})}}document.addEventListener('DOMContentLoaded',()=>{setTimeout(createParticles,100);setupSlider();setupScrollEffect();setupSectionSwitching();setupTouchNavigation();setupFooter();setupMobileMenu();setupMobileScrollSpy();let resizeTimeout;window.addEventListener('resize',function(){clearTimeout(resizeTimeout);resizeTimeout=setTimeout(setupMobileScrollSpy,250)})});
}();